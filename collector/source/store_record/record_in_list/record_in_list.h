#ifndef RECORD_IN_LIST_H
#define RECORD_IN_LIST_H

#include "../../read_access_record/read_access_record.h"
#include <vector>

bool record_in_list(access_record record, std::vector<access_record> list);

#endif